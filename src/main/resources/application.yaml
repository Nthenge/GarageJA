spring:
  datasource:
    url: jdbc:mysql://localhost:3306/garage?useSSL=false
    username: root
    password: 5047

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 150MB

  jpa:
    properties:
      hibernate:
        dialect:
          org.hibernate.dialect.MySQL8Dialect

  jpa.hibernate.ddl_auto: update

server:
  address: 0.0.0.0
  port: 8083
  ssl:
    enabled: false

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
