spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/garage
    username: postgres
    password: 5047
#    url: ${SPRING_DATASOURCE_URL} for production
#    username: ${SPRING_DATASOURCE_USERNAME}
#    password: ${SPRING_DATASOURCE_PASSWORD}
  flyway:
    enabled: false  # return this to true and add migration scripts
  servlet:
    multipart:
      enabled: true
      max-file-size: 200MB
      max-request-size: 250MB

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  jpa.hibernate.ddl-auto: update

server:
  address: 0.0.0.0
  port: 8083
  ssl:
    enabled: false

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

  mpesa:
    consumer:
      key: rrS42JDcIh7jGmmBsSOahnHWPkRYmK84MbwUcygtH9vQUsj0
      secret: pm08BgNWThsd46UOVeAYkWf8C0hClfLGc5r6HyAVwNZsdJGCWDRdqF83kjf5Pz2W
    passkey:
    business:
      shortcode: 174379
    base:
      url: https://sandbox.safaricom.co.ke
    callback:
      url: https://yourdomain.com/api/mpesa/callback
