spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/garage
    username: postgres
    password: 5047
    hikari:
      auto-commit: false
  cache:
    type: simple

  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: 51c2c6a995c556
    password: 2780c1bfbb46ad
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  flyway:
    enabled: false  # set to true add migration scripts later

  servlet:
    multipart:
      enabled: true
      max-file-size: 200MB
      max-request-size: 250MB

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: public
        hibernate:
          cache:
            use_second_level_cache: true
            use_query_cache: true
            region.factory_class: org.hibernate.cache.jcache.JCacheRegionFactory
            provider: org.ehcache.jsr107.EhcacheCachingProvider

server:
  address: 0.0.0.0
  port: 8083
  ssl:
    enabled: false
  compression:
    enabled: true
    mime-types: text/html,text/xml,text/plain,application/json
    min-response-size: 1024

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
